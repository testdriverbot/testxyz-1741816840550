version: 4.2.18
session: 67d206616c034d81f8c8ba87
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Reload the current page.
    commands:
      - command: press-keys
        keys:
          - f5
  - prompt: >-
      Assert that DNS issues are properly reported in the error message.  # Use
      the content of the file from the environment
    commands:
      - command: assert
        expect: DNS_PROBE_FINISHED_NXDOMAIN is visible on the error page
